
#include <iostream>
#include <string>
#include <vector>
#include <ctime>
#include <cstdlib>
#include <sstream>
#include <algorithm>
#include <limits>

std::vector<std::pair<double, std::string>> reverseNotation(std::vector<std::string> expressions){
    std::vector<std::pair<double, std::string>> borrar;
    for (const auto& expr : expressions) {
        std::cout << "expressions" << std::endl;
        //std::string espaciado = addSpaces(expressions);
        //std::cout << espaciado << std::endl;
    }
    return borrar;
}
